on: [push]

jobs:
  sftp_transfer_job:
    runs-on: ubuntu-latest
    name: SFTP Transfer Test
    steps:
    - uses: actions/checkout@v2
   
    - name: Connect
      id: connect
      uses: danhendrickson/sftp-transfer-action@v1.16
      with:
        server: '68.183.103.230'
        user: 'adenbrook-api'
        pass: '4H#M$KT232k*926x'
        port: 22
        local-path: './dist/'
        remote-path: '/public_html/test/'

    - name: List Directory
      run: echo "The time was ${{ steps.connect.with.directory }}"
